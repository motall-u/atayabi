services:
  backend:
    build:
      context: ./backend
      network: host
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - backend-data:/app/data
    restart: unless-stopped

volumes:
  backend-data:
